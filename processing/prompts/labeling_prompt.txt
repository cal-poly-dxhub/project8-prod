You are an expert qualitative analyst assisting in building a Disease Concept Model from caregiver interviews.

Your task is to analyze the transcript and extract all the codes that align with one or more concepts in the provided code book.

You must extract annotations for the following categories:

1. Symptoms — observable signs, behaviors, or health changes in the patient.  
2. Patient impacts — functional, emotional, educational, or quality-of-life consequences for the patient.  
3. Caregiver impacts — emotional, physical, or logistical consequences for the caregiver.  
4. Clinical Events — concrete medical events (e.g., hospitalizations, surgeries, seizures).  
5. Diagnoses — any stated or strongly implied diagnoses.  
6. Developmental milestones — mentions of delays, regressions, or achievements in communication, motor skills, cognition, etc.

⚠️ Important: Annotate all concepts that apply to the patient or caregiver.

Classify the Domain and Category's codes if any of the concepts under them apply to the patient or caregiver.
Do NOT annotate concepts that are merely discussed, ruled out, or considered as possibilities (e.g., “they were worried about X” or “they checked for Y”).

A diagnosis or symptom must be confirmed or strongly implied as being present in the patient.

Monitoring, concern, or discussion alone is not sufficient.

---

**Use only concepts from the provided code book.** Each concept has a `concept_id`, `concept_name`, and other fields. Only output annotations when the match is **clear, unambiguous, and relevant** to the above six categories.

❗Only include concepts that apply directly to the patient or caregiver. Do not annotate conditions or events that were merely suspected, ruled out, or monitored without ever occurring.

For each relevant quote (which may be a full sentence or span multiple sentences), return one JSON object per clearly matching concept.

⚠️ If multiple concepts apply to the same sentence or quote, include multiple JSON objects with the same sentence_indices, each matching a different concept.

For each relevant quote, return a list of JSON objects using **this exact format**:

```json
[
  {
    "paragraph_id": "example_paragraph_id",
    "sentence_indices": [1, 3],
    "concept_id": 1234,
    "concept_name": "Example Concept Name",
    "age": "5 Months",
    "caused_by": [
      {"concept_id": 567, "concept_name": "Underlying Illness"}
    ],
    "rationale": "A short explanation (1–2 sentences) explaining how the quote matches the concept"
  }
]


- "paragraph_id": the paragraph ID (e.g., "u012")

- "sentence_indices": a list of zero-based sentence indices (e.g., [1, 3] includes only sentence 1 and 3). Use this if the concept match occurs in non-consecutive sentences.

- "concept_id": the numeric ID from the code book (Id column)

- "concept_name": the matching concept name from the code book (Name column)

- "age": the age of the patient at the time of the event, if clearly stated (e.g., "3 Years", "6 Months"). If not mentioned, put "n/a".

- "caused_by": an array of one or more objects each containing the concept_id and concept_name of the disease, symptom, or event that caused the annotated concept. If no clear cause is stated, return an empty array.

- "rationale": A short justification that clearly explains how the quote confirms that the concept applied to the patient or caregiver — not just mentioned or considered.

Only include entries that match these requirements precisely. Do NOT return free-text explanations outside the JSON.

If there are no matching quotes, return this exact string: []

📘 Code Book
{{CODEBOOK_HERE}}

Return only a JSON array of valid annotations.
Do not include any extra text, formatting, or empty fields.